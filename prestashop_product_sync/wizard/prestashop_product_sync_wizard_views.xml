<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_prestashop_product_sync_wizard_form" model="ir.ui.view">
        <field name="name">prestashop.product.sync.wizard.form</field>
        <field name="model">prestashop.product.sync.wizard</field>
        <field name="arch" type="xml">
            <form string="Product Sync">

                <!-- Progress bar -->
                <field name="state" invisible="1"/>
                <group invisible="state == 'draft'">
                    <field name="progress" widget="progressbar"/>
                </group>

                <!-- Stats -->
                <group invisible="state == 'draft'" col="4">
                    <field name="current" string="Current"/>
                    <field name="total" string="Total"/>
                    <field name="created_count" string="Created"/>
                    <field name="updated_count" string="Updated"/>
                    <field name="error_count" string="Errors"/>
                </group>

                <!-- Config (only in draft) -->
                <group invisible="state != 'draft'">
                    <field name="instance_id" readonly="1"/>
                    <field name="product_limit"/>
                </group>

                <!-- Log area -->
                <group string="Sync Log" invisible="state == 'draft'">
                    <field name="log" nolabel="1"/>
                </group>

                <footer>
                    <button name="action_start_sync"
                            string="Start Sync"
                            type="object"
                            class="btn-primary"
                            invisible="state != 'draft'"/>
                    <button string="Close"
                            class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_prestashop_product_sync_wizard" model="ir.actions.act_window">
        <field name="name">Test Product Sync</field>
        <field name="res_model">prestashop.product.sync.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="prestashop_odoo_sync.model_prestashop_instance"/>
    </record>

</odoo>
