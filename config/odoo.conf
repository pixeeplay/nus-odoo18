[options]
; ===========================================
; Odoo 18 Enterprise - Configuration
; odoo.productsmanager.app
; Database: doscaal (client import)
; ===========================================

; --- Connexion Base de données ---
db_host = db
db_port = 5432
db_user = odoo
db_password = ProductsManagerOdoo18SecureProd2025X
db_name = doscaal
db_maxconn = 128
db_template = template0

; --- Paths ---
addons_path = /mnt/extra-addons,/usr/lib/python3/dist-packages/odoo/addons
data_dir = /var/lib/odoo

; --- Sécurité ---
admin_passwd = Nusmob2710$
list_db = True
without_demo = all

; --- Performance (8GB+ RAM - mode production) ---
workers = 8
max_cron_threads = 4
limit_memory_hard = 5368709120
limit_memory_soft = 3221225472
limit_time_cpu = 1200
limit_time_real = 2400
limit_time_real_cron = 7200
limit_request = 8192

; --- HTTP ---
http_enable = True
http_interface = 0.0.0.0
http_port = 8069
longpolling_port = 8072
proxy_mode = True

; --- Sessions & Cache ---
server_wide_modules = base,web

; --- Logging ---
log_level = info
log_handler = :INFO
log_db = False
log_db_level = warning

; --- Divers ---
csv_internal_sep = ,
