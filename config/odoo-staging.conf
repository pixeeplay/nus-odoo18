[options]
; ===========================================
; Odoo 18 Enterprise - Configuration STAGING
; staging-odoo.productsmanager.app
; ===========================================

; --- Connexion Base de données ---
db_host = db
db_port = 5432
db_user = odoo
db_password = ProductsManagerOdoo18SecureStaging2025X
db_name = False
db_maxconn = 64
db_template = template0

; --- Paths ---
addons_path = /mnt/extra-addons,/usr/lib/python3/dist-packages/odoo/addons
data_dir = /var/lib/odoo

; --- Sécurité ---
admin_passwd = Nusmob2710$
list_db = True
without_demo = all

; --- Performance (mode staging - ressources réduites) ---
workers = 4
max_cron_threads = 2
limit_memory_hard = 2684354560
limit_memory_soft = 1610612736
limit_time_cpu = 1200
limit_time_real = 2400
limit_time_real_cron = 7200
limit_request = 4096

; --- HTTP ---
http_enable = True
http_interface = 0.0.0.0
http_port = 8069
longpolling_port = 8072
proxy_mode = True

; --- Sessions & Cache ---
server_wide_modules = base,web

; --- Logging (plus verbeux pour debug staging) ---
log_level = debug
log_handler = :INFO,odoo.modules:DEBUG,odoo.addons:DEBUG
log_db = False
log_db_level = warning

; --- Divers ---
csv_internal_sep = ,
